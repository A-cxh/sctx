;$(".banner1").banner({
    imgs: $(".banner1").find(".img"),         //必选
    left: $(".banner1").find("#left"),        //可选
    right: $(".banner1").find("#right"),      //可选
    list: true,                               //可选，默认存在
    index: 0,                                 //可选，默认0
    autoPlay: true,                           //可选，默认true
    delayTime: 5000,                          //可选，默认2000
    moveTime: 1000                            //可选，默认400
});
;$(".banner2").banner({
    imgs: $(".banner2").find(".img"),         //必选
    left: $(".banner2").find("#left"),        //可选
    right: $(".banner2").find("#right"),      //可选
    list: false,                              //可选，默认存在
    index: 0,                                 //可选，默认0
    autoPlay: true,                           //可选，默认true
    delayTime: 4000,                          //可选，默认2000
    moveTime: 400                             //可选，默认400
});
;$(".banner3").banner({
    imgs: $(".banner3").find(".img"),         //必选
    list: true,                               //可选，默认存在
    index: 0,                                 //可选，默认0
    autoPlay: true,                           //可选，默认true
    delayTime: 4000,                          //可选，默认2000
    moveTime: 400                             //可选，默认400
});
;$(".banner4").bannerRed({
    imgs: $(".banner4").find(".img"),         //必选
    list: true,                               //可选，默认存在
    index: 0,                                 //可选，默认0
    autoPlay: true,                           //可选，默认true
    delayTime: 5000,                          //可选，默认2000
    moveTime: 800                             //可选，默认400
});
;$(".banner5").banner({
    imgs: $(".banner5").find(".img"),         //必选
    list: true,                               //可选，默认存在
    index: 0,                                 //可选，默认0
    autoPlay: true,                           //可选，默认true
    delayTime: 3500,                          //可选，默认2000
    moveTime: 400                             //可选，默认400
});
;$(".banner6").banner({
    imgs: $(".banner6").find(".img"),         //必选
    list: true,                               //可选，默认存在
    index: 0,                                 //可选，默认0
    autoPlay: true,                           //可选，默认true
    delayTime: 4000,                          //可选，默认2000
    moveTime: 400                             //可选，默认400
});
;$(".banner7").bannerRed({
    imgs: $(".banner7").find(".img"),         //必选
    list: true,                               //可选，默认存在
    index: 0,                                 //可选，默认0
    autoPlay: true,                           //可选，默认true
    delayTime: 5000,                          //可选，默认2000
    moveTime: 800                             //可选，默认400
});
;$(".banner8").banner({
    imgs: $(".banner8").find(".img"),         //必选
    list: true,                               //可选，默认存在
    index: 0,                                 //可选，默认0
    autoPlay: true,                           //可选，默认true
    delayTime: 4000,                          //可选，默认2000
    moveTime: 400                             //可选，默认400
});
;$(".banner9").bannerRed({
    imgs: $(".banner9").find(".img"),         //必选
    list: true,                               //可选，默认存在
    index: 0,                                 //可选，默认0
    autoPlay: true,                           //可选，默认true
    delayTime: 5000,                          //可选，默认2000
    moveTime: 800                             //可选，默认400
});
;$(".banner10").banner({
    imgs: $(".banner10").find(".img"),        //必选
    left: $(".banner10").find("#left"),       //可选
    right: $(".banner10").find("#right"),     //可选
    list: false,                              //可选，默认存在
    index: 0,                                 //可选，默认0
    autoPlay: true,                           //可选，默认true
    delayTime: 5000,                          //可选，默认2000
    moveTime: 800                             //可选，默认400
});